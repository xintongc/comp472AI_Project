class Half:
    def __init__(self, name, num):
        self.name = name  # name as WO / RX
        self.num = num  # indicate which two half are one card

    # def match_with_role_token(self, role_token):
    #     if role_token == self.color or role_token == self.dot:
    #         return True
    #     else:
    #         return False

